1. git init
2. git add .
3. git commit -m "first commit"
4. git remote add origin https://github.com/AbdulazizAlmalki/Java.git

Example:
git remote add origin https://github.com/AbdulazizAlmalki/Java.git
5. git push -u origin master
6. you can check in remote repository

If you can add any file in local repository or you can change the existing files in local repository

1. git status
2. git add .
3. git commit -m "second commit"
4. git push -u origin master or git push origin master


If your want to retrieve the remore repo to local pc

git clone https://github.com/AbdulazizAlmalki/Java.git


